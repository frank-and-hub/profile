'use client';
import { useEffect } from 'react';
import { FaCode, FaServer, FaDatabase, FaCloud } from 'react-icons/fa';

const services = [
    {
        icon: FaCode,
        title: "Full-stack Development",
        description: "End-to-end development using React/Next.js and Node.js/Express.",
        variant: "default"
    },
    {
        icon: FaServer,
        title: "API Development",
        description: "Building RESTful APIs and GraphQL services with authentication.",
        variant: "dark"
    },
    {
        icon: FaDatabase,
        title: "Database Architecture",
        description: "Designing scalable database solutions and managing migrations.",
        variant: "default"
    },
    {
        icon: FaCloud,
        title: "DevOps & Cloud",
        description: "Setting up CI/CD, Docker, and cloud deployments.",
        variant: "default"
    }
];

export default function Service() {
    useEffect(() => {
        if (typeof window !== 'undefined') {
            const WOW = require('wow.js');
            new WOW().init();
        }
    }, []);

    return (
        <section id="service" className="parallax-section">
            <div className="container">
                <div className="section-title wow fadeInUp" data-wow-delay="0.2s">
                    <h2>What I Can Do For You</h2>
                    <p>Specialized in full-stack development with a focus on scalable solutions</p>
                </div>
                <div className="services-grid">
                    {services.map((service, index) => (
                        <div
                            key={service.title}
                            className={`service-item wow fadeInUp ${service.variant === 'dark' ? 'dark' : ''}`}
                            data-wow-delay={`${0.2 * (index + 1)}s`}
                        >
                            <div className="service-icon">
                                <service.icon size={30} />
                            </div>
                            <h4>{service.title}</h4>
                            <p>{service.description}</p>
                        </div>
                    ))}
                </div>
            </div>
        </section>
    );
}

                            key={service.title}

                            className={`service-item wow fadeInUp ${service.variant === 'dark' ? 'dark' : ''}`}        {

                            data-wow-delay={`${0.2 * (index + 1)}s`}

                        >            icon: FaCloud,        icon: FaServer,

                            <div className="service-icon">

                                <service.icon size={30} />            title: "DevOps & Cloud",

                            </div>

                            <h4>{service.title}</h4>            description: "Setting up CI/CD pipelines, containerization with Docker, and cloud deployment on AWS/GCP.",        title: "API Development",    {

                            <p>{service.description}</p>

                        </div>            variant: "default"

                    ))}

                </div>        }        description: "Building robust RESTful APIs and GraphQL services with authentication, optimization, and documentation.",

            </div>

        </section>    ];

    );

}        variant: "dark"        icon: FaCode,import '../styles/services.css';    {

    useEffect(() => {

        if (typeof window !== 'undefined') {    },

            const WOW = require('wow.js');

            new WOW().init();    {        title: "Full-stack Development",

        }

    }, []);        icon: FaDatabase,



    return (        title: "Database Architecture",        description: "End-to-end application development using React/Next.js on the frontend and Node.js/Express on the backend.",        title: "Full-stack Development",

        <section id="service" className="parallax-section">

            <div className="container">        description: "Designing and implementing scalable database solutions, optimizing queries, and managing data migrations.",

                <div className="section-title wow fadeInUp" data-wow-delay="0.2s">

                    <h2>What I Can Do For You</h2>        variant: "default"        variant: "default"

                    <p>Specialized in full-stack development with a focus on scalable and maintainable solutions</p>

                </div>    },



                <div className="services-grid">    {    },const services = [        description: "End-to-end application development using React/Next on the frontend and Node/Nest on the backend.",

                    {services.map((service, index) => (

                        <div         icon: FaCloud,

                            key={service.title}

                            className={`service-item wow fadeInUp ${service.variant === 'dark' ? 'dark' : ''}`}        title: "DevOps & Cloud",    {

                            data-wow-delay={`${0.2 * (index + 1)}s`}

                        >        description: "Setting up CI/CD pipelines, containerization with Docker, and cloud deployment on AWS/GCP.",

                            <div className="service-icon">

                                <service.icon size={30} />        variant: "default"        icon: FaServer,    {        icon: FaCode,

                            </div>

                            <h4>{service.title}</h4>    }

                            <p>{service.description}</p>

                        </div>];        title: "API Development",

                    ))}

                </div>

            </div>

        </section>export default function Service() {        description: "Building robust RESTful APIs and GraphQL services with authentication, optimization, and documentation.",        icon: FaCode,        gradient: "service-gradient-blue"

    );

}    useEffect(() => {

        if (typeof window !== 'undefined') {        variant: "dark"

            const WOW = require('wow.js');

            new WOW().init();    },        title: "Full-stack Development",    },

        }

    }, []);    {



    return (        icon: FaDatabase,        description: "End-to-end application development using React/Next.js on the frontend and Node.js/Express on the backend.",    {

        <section id="service" className="parallax-section">

            <div className="container">        title: "Database Architecture",

                <div className="section-title wow fadeInUp" data-wow-delay="0.2s">

                    <h2>What I Can Do For You</h2>        description: "Designing and implementing scalable database solutions, optimizing queries, and managing data migrations.",        variant: "default"        title: "Backend & API Development",

                    <p>Specialized in full-stack development with a focus on scalable and maintainable solutions</p>

                </div>        variant: "default"



                <div className="services-grid">    },    },        description: "Designing and implementing RESTful and GraphQL APIs, authentication and business logic.",

                    {services.map((service, index) => (

                        <div     {

                            key={service.title}

                            className={`service-item wow fadeInUp ${service.variant === 'dark' ? 'dark' : ''}`}        icon: FaCloud,    {        icon: FaServer,

                            data-wow-delay={`${0.2 * (index + 1)}s`}

                        >        title: "DevOps & Cloud",

                            <div className="service-icon">

                                <service.icon size={30} />        description: "Setting up CI/CD pipelines, containerization with Docker, and cloud deployment on AWS/GCP.",        icon: FaServer,        gradient: "service-gradient-purple"

                            </div>

                            <h4>{service.title}</h4>        variant: "default"

                            <p>{service.description}</p>

                        </div>    }        title: "API Development",    },

                    ))}

                </div>];

            </div>

        </section>        description: "Building robust RESTful APIs and GraphQL services with authentication, optimization, and documentation.",    {

    );

}export default function Service() {

    useEffect(() => {        variant: "dark"        title: "Databases & Storage",

        if (typeof window !== 'undefined') {

            const WOW = require('wow.js');    },        description: "Database schema design, migrations, and performance tuning for SQL and NoSQL databases.",

            new WOW().init();

        }    {        icon: FaDatabase,

    }, []);

        icon: FaDatabase,        gradient: "service-gradient-orange"

    return (

        <section id="service" className="parallax-section">        title: "Database Architecture",    },

            <div className="container">

                <div className="section-title wow fadeInUp" data-wow-delay="0.2s">        description: "Designing and implementing scalable database solutions, optimizing queries, and managing data migrations.",    {

                    <h2>What I Can Do For You</h2>

                    <p>Specialized in full-stack development with a focus on scalable and maintainable solutions</p>        variant: "default"        title: "DevOps & Deployment",

                </div>

    },        description: "Containerization, CI/CD pipelines, cloud deployments and monitoring (Docker, AWS, etc.).",

                <div className="services-grid">

                    {services.map((service, index) => (    {        icon: FaCloud,

                        <div 

                            key={service.title}        icon: FaCloud,        gradient: "service-gradient-green"

                            className={`service-item wow fadeInUp ${service.variant === 'dark' ? 'dark' : ''}`}

                            data-wow-delay={`${0.2 * (index + 1)}s`}        title: "DevOps & Cloud",    }

                        >

                            <div className="service-icon">        description: "Setting up CI/CD pipelines, containerization with Docker, and cloud deployment on AWS/GCP.",];

                                <service.icon size={30} />

                            </div>        variant: "default"

                            <h4>{service.title}</h4>

                            <p>{service.description}</p>    }export default function Service() {

                        </div>

                    ))}];    return (

                </div>

            </div>        <>

        </section>

    );export default function Services() {            <section id="service" className="services-section">

}
    useEffect(() => {                <div className="container">

        if (typeof window !== 'undefined') {                    <div className="services-header fade-in">

            const WOW = require('wow.js');                        <h2 className="services-title">

            new WOW().init();                            My Services

        }                        </h2>

    }, []);                        <p className="services-description">

                            I offer a range of services to help bring your digital projects to life,

    return (                            focusing on modern web development and design.

        <section id="service" className="parallax-section">                        </p>

            <div className="container">                    </div>

                <div className="section-title wow fadeInUp" data-wow-delay="0.2s">

                    <h2>What I Can Do For You</h2>                    <div className="services-grid">

                    <p>Specialized in full-stack development with a focus on scalable and maintainable solutions</p>                        {services.map((service, index) => (

                </div>                            <div

                                key={service.title}

                <div className="services-grid">                                className="service-card hover-lift hover-glow slide-up"

                    {services.map((service, index) => (                                style={{ animationDelay: `${index * 100}ms` }}

                        <div                             >

                            key={service.title}                                <div className={`service-icon ${service.gradient}`}>

                            className={`service-item wow fadeInUp ${service.variant === 'dark' ? 'dark' : ''}`}                                    <service.icon size={26} />

                            data-wow-delay={`${0.2 * (index + 1)}s`}                                </div>

                        >

                            <div className="service-icon">                                <h3 className="service-card-title">

                                <service.icon size={30} />                                    {service.title}

                            </div>                                </h3>

                            <h4>{service.title}</h4>                                <p className="service-card-description">

                            <p>{service.description}</p>                                    {service.description}

                        </div>                                </p>

                    ))}                            </div>

                </div>                        ))}

            </div>                    </div>

        </section>                </div>

    );            </section>

}        </>
    );
}
