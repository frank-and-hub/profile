# admin_back_end

have to run 
npm install @faker-js/faker axios  bcrypt  body-parser cors date-fns dotenv express express-vali dator fs jsonwebtoken mongoos mongoose  mongoose-seed morgan multer nodemailer nodemon path sharp stack-trace
